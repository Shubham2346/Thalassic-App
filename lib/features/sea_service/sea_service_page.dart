import 'package:flutter/material.dart';

class SeaServicePage extends StatelessWidget {
  const SeaServicePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Sea Service')),
      body: Center(child: Text('Sea Service Page')),
    );
  }
}